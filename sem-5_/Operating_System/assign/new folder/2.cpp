Void Philosopher
{
    while (1)
    {
        take_chopstick[i];
        take_chopstick[(i + 1) % 5];

        // EATING THE NOODLE

   put_chopstick([i] );
   put_chopstick[(i + 1) % 5];
   .
       .THINKING
    }
}
semaphore C[5];

down(S)
{
    while (S <= 0)
   ;
    S--;
}

up(S)
{
    S++;
}

#include <bits/stdc++.h>

void Philosopher
{
    while (1)
    {
   down(take_chopstickC[i]);
   down(take_chopstickC[(i + 1) % 5]);

   // EATING THE NOODLE

   up(put_chopstickC[i]);
   up(put_chopstickC[(i + 1) % 5]);
   // THINKING
    }
}

int main()
{
    int p = 5;
    int c= 5;  

}